{
  "name": "mcp-supabase",
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "bin": {
    "mcp-supabase": "build/index.js"
  },
  "files": [
    "build"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x build/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.6.0",
    "@supabase/supabase-js": "^2.39.3",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "shx": "^0.3.4"
  }
}
